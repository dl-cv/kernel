# ISP 手动白平衡方案（审核稿）

日期：2026-08-26  
范围：kernel（rkisp）+ CamOS 后端/设置页 + GigE/MVS  
状态：待审核，未改代码

## 要解决什么

现在改白平衡是 CamOS 往 `/proc/rkisp*` 里 echo 寄存器（`mode=0x100`、`400=0x6197`、`538/53c`）。这和曝光/增益不一样：曝光/增益走 V4L2 控件，白平衡走调试后门。

风险：ISP 没起来时写会 SError；任意偏移可写；寄存器在 ISP 重建后丢，冷启动第一枪容易整幅偏绿。

## 改成什么样

在 **ISP 节点**（不是 IMX296 sensor）上提供手动白平衡，用法对齐曝光/增益：`v4l2-ctl` / ioctl 可 get/set。CamOS、设置页、MVS 都改成「1.0 为中性」。

不接 rkaiq，不做内核自动白平衡，不写 HDR 的第二/第三组 gain。

## 人能看到的变化

| 位置 | 现在 | 改完 |
|---|---|---|
| 设置页 R/G/B | 默认 1024 | 默认 **1.000**（倍率） |
| MVS BalanceRatio | 100 或 1024，对不上 1.0 | **1.000** |
| 内核 | 无 WB 控件 | ISP subdev 上 4 项：自动开关 + R/G/B |
| 下发路径 | 写 `/proc` | 写 V4L2 控件 |

出厂校正色（现 1610/1024/2207）换算后约为 **1.572 / 1.000 / 2.155**。已保存的用户集会自动迁一次。

## 内核挂 4 个控件

1. 白平衡自动：默认关（本轮开自动直接失败，不做 AWB 算法）
2. 红增益
3. 绿增益（标准 V4L2 没有绿，必须自定义一项，否则 CamOS 三通道对不上）
4. 蓝增益

数值：整数，**1000 = 1.0 倍**，范围 1–16000（0.001×–16×）。0 不允许。

不挂：色温、一键白平衡按钮、HDR extra gain。一键白平衡仍在 CamOS 里用预览均值算，只换下发通道。

## 三仓分工

- **kernel**：真正写 ISP AWBGAIN0（地址 0x538/0x53c），stream-on 后自动重放缓存，避免冷启动丢 WB。
- **CamOS**：不再默认写 proc；sensor 节点继续管曝光/增益，WB 走 ISP 节点。设置页显示 1.000。
- **GigE**：MVS 看到 Float 1.000，内部仍存整数 1000。

## 建议批的点

1. 单位用 **1.000=1000**，设置页和 MVS 一起改，不要内核改了前端还显示 1024。
2. 只做手动三路 gain，不做内核 AWB / HDR。
3. `/proc` 保留调试，产品路径默认关掉。

## 风险

- 要换内核才能在板上看到新控件。
- 旧用户集迁错单位会偏色（用标记字段只迁一次，可抽查 1610→1572）。
- MVS 若仍按 Integer 100 理解，属性页会乱；所以 XML 必须改成 Float。
- ISP 未开流时 set 应报错，不能再 panic。

## 怎么验收（短）

- `v4l2-ctl` 能列出 4 项，默认 1000。
- 设 1.0 后画面中性；拉红/蓝能看出变化。
- 设置页默认 1.000；MVS 也是 1.000。
- 冷启动第一枪不再整幅橄榄绿。
- `dmesg` 不再出现产品路径的 `mode=0x100`。

## 给实现用的详细稿

同目录：`IMX296_ISP手动白平衡_给AI_20260826.md`
