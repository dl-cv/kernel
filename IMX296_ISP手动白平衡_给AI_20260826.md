# ISP 手动白平衡：给实现 AI 的完整计划

日期：2026-08-26  
工作树：`/home/okay/Desktop/kernel_worktrees/fix_imx296_n1_pwm`  
CamOS：`/home/okay/Desktop/CamOS`  
状态：方案已定，按本文实现。不要再改单位、不要加 HDR、不要把 WB 挂到 IMX296。

配套给人看的短稿：同目录 `IMX296_ISP手动白平衡_给人审核_20260826.md`。

---

## 0. 目标与非目标

**做：**

1. 在 rkisp **ISP subdev** 上提供手动 WB gain，用法对齐 IMX296 的 exposure/gain（ioctl / `v4l2-ctl`）。
2. 全链路单位：**1.000 倍 = 整数 1000**。CamOS WS、设置页、GigE/MVS 一起改，禁止再以 1024 或 100 当中性点。
3. CamOS 默认停止往 `/proc/rkisp*` echo 寄存器。

**不做：**

- 不走 `rkisp-input-params` 整包 IQ（CamOS 无 rkaiq）。
- 不实现内核 AWB 统计闭环；`white_balance_automatic=1` 返回 `-EINVAL`。
- 不写 HDR GAIN1/GAIN2。
- 不把 WB 控件挂到 `imx296.c`。
- 不加长 warm/gate/quarantine，不用 drop 吞首枪。
- 不扩展 `/proc` 白名单当正式 API。
- commit 不要带 `Co-Authored-By: Claude`。

---

## 1. 现状（改之前必须理解）

### 1.1 对照路径

```
曝光/增益:
  WS → GstV4L2Grabber → linuxpy VIDIOC_S_CTRL
    → IMX296 subdev  V4L2_CID_EXPOSURE / V4L2_CID_ANALOGUE_GAIN
  FT 下 EXPOSURE 是 no-op，曝光走 sysfs trigger_pulse_us

白平衡（现状，要废）:
  WS rkcam_set_balance_ratio {r,g,b}   # 1024 = 1.0
    → gst_v4l2_controls._apply_control_one("set_balance_ratio")
    → _write_wb_register_from_ratio()     # ~528
    → open("/proc/rkisp*-vir*", "w")
    → "mode=0x100\n" + "400=0x6197\n" + "538=0xGGGGGGGG 53c=0xRRRRBBBB\n"
```

测试明确优先 proc：`tests/camera/test_gain_refresh_and_roi_recovery.py` 的
`test_set_balance_ratio_does_not_fallback_to_v4l2_controls`。

文档：`CamOS/docs/18_RKISP白平衡寄存器地址映射.md`。

### 1.2 硬件落点

WB 在 ISP，不在 sensor。

| 项 | 值 |
|---|---|
| GAIN0 G | `ISP3X_ISP_AWB_GAIN0_G` = `ISP3X_ISP_BASE(0x400)+0x138` = **0x538** |
| GAIN0 RB | `ISP3X_ISP_AWB_GAIN0_RB` = **0x53c** |
| enable | `ISP3X_ISP_CTRL0` bit `CIF_ISP_CTRL_ISP_AWB_ENA` |
| pack | `ISP_PACK_2SHORT(a,b) = (a & 0xffff) \| ((b & 0xffff) << 16)` |
| 官方写 | `isp_awbgain_config()`：`G = PACK_2SHORT(g_b, g_r)`，`RB = PACK_2SHORT(b, r)` |
| 与 CamOS 现 pack 对齐 | `reg_g = (g<<16)\|g`，`reg_rb = (r<<16)\|b`（线性机 g_r=g_b） |
| 零值 | 任一通道 0 → `awb gain is zero!` 整包丢弃 |

CamOS 实测 RK3576 只有 538/53c 有效。本方案只写 **GAIN0**。

### 1.3 proc 后门为什么危险

`drivers/media/platform/rockchip/isp/procfs.c`：

- `rkisp_proc_write()`：`mode=0x100` 置 `RKISP_PROCFS_FIL_AIQ`，之后任意 `reg=val` MMIO。
- `rkisp_proc_hw_ready_for_mmio()`：未 `ISP_START` 时写 FIL_AIQ/FIL_SW 会 Asynchronous SError（注释已点名 CamOS WB）。
- `isp_params.c`：`FIL_AIQ|FIL_SW` 时后续 params buf 直接 `VB2_BUF_STATE_DONE`，IQ 丢掉。
- 寄存器无持久化 → close→FT 重建 ISP 后 AWBGAIN 回默认 → 冷启动首枪橄榄绿。

### 1.4 现有 handler 空位

- IMX296：`imx296_ctrls_init()` 有 exposure/gain，无 WB。对。
- rkisp：`dev.c` 里 `v4l2_ctrl_handler_init(&isp_dev->ctrl_handler, 5)` **没挂 control**。
- ISP subdev：`struct rkisp_isp_subdev` 有 `ctrl_handler`（`rkisp.h`），`rkisp_register_isp_subdev()` **未 init、未挂到 sd**。`rkisp_isp_sd_ops` 无 `s_ctrl`。
- CamOS `native_v4l2/controls.py` `_has_sensor_controls` 把 exposure 和 red_balance 混在一个节点上选。板上实际没有 red_balance，所以一直走 proc。

---

## 2. 单位契约（唯一真源）

| 层 | 中性 1.0 | 范围 | 备注 |
|---|---|---|---|
| 内核 V4L2 | **1000** | **1–16000** step 1 | 禁止 0 |
| CamOS WS / 状态 / 用户集 | 同上整数 | 同上 | `wb_ratio: {r,g,b}` |
| 设置页 | **1.000** | **0.001–16.000** step 0.001 | 提交 `round(x*1000)` |
| GigE `BalanceRatio` | **1.0** IFloat | 0.001–16.0 | 寄存器存 milli-int |
| ISP Q8.8 | 256 | `hw = val * 256 / 1000` | 1000 → 256 |

出厂校正（旧 1024 基 `{1610,1024,2207}`）→ milli：

```
r = round(1610 * 1000 / 1024) = 1572
g = round(1024 * 1000 / 1024) = 1000
b = round(2207 * 1000 / 1024) = 2155
```

内核换算用整数：`hw = (u32)val * 256 / 1000`（注意 32-bit）。

用户集：增加 `wb_ratio_unit: "milli"`。若缺该字段且存在 `wb_ratio`，按 1024 基换算后回写。只迁一次。`g/r/b` 缺省键兼容 `red/green/blue`。

---

## 3. Kernel 实现

### 3.1 文件

主改：

- `drivers/media/platform/rockchip/isp/rkisp.c`
- `drivers/media/platform/rockchip/isp/rkisp.h`

必要时只读 reuse：

- `isp_params.h` 的 `ISP_PACK_2SHORT`
- `isp_params_v3x.c` / `isp_params_v39.c` 的 `isp_awbgain_config` 作为 pack 对照（不要为了手动 WB 去改 params 队列）

不要改 `imx296.c`。不要扩 `procfs.c` 写入口。

### 3.2 CID

在 `rkisp.c`（或一小段 uapi/本地头）定义：

```
#ifndef V4L2_CID_USER_RKISP_BASE
#define V4L2_CID_USER_RKISP_BASE		(V4L2_CID_USER_BASE + 0x11c0)
#endif
#define V4L2_CID_USER_RKISP_GREEN_BALANCE	(V4L2_CID_USER_RKISP_BASE + 0)

/* 标准 */
V4L2_CID_AUTO_WHITE_BALANCE
V4L2_CID_RED_BALANCE
V4L2_CID_BLUE_BALANCE
```

`0x11c0` 避开本树已用的 `IMX296 0x10d0`、主线 `0x10b0`/`0x10e0`/`0x1170`/`0x1180`/`0x1190`。实现前再 grep 一次冲突。

`v4l2_ctrl_new_std` 对 RED/BLUE/AUTO 即可。GREEN 用 `v4l2_ctrl_new_custom`，`name = "green_balance"`，type INTEGER。

范围：min=1, max=16000, step=1, def=1000。  
AUTO：min=0, max=1, step=1, def=**0**。

### 3.3 挂到 ISP subdev

在 `rkisp_register_isp_subdev()`（约 `rkisp.c:4720`）中：

1. `v4l2_ctrl_handler_init(&isp_sdev->ctrl_handler, 4)`
2. 创建 4 个 control，ops = `rkisp_wb_ctrl_ops`
3. `sd->ctrl_handler = &isp_sdev->ctrl_handler`
4. handler lock：用现有 ISP 侧能串行化 MMIO 的锁（`iqlock` / `apilock`，选一个并在注释写清）。不要和 sensor mutex 混用。

`rkisp_isp_sd_ops.core` 需要能走 subdev ctrl（标准路径是 `sd->ctrl_handler` + v4l2-ctrl 框架；按本树 subdev 惯例接 `s_ctrl`）。

卸载路径 `rkisp_unregister_isp_subdev()` 里 `v4l2_ctrl_handler_free`。

### 3.4 状态缓存

在 `struct rkisp_device` 或 `rkisp_isp_subdev` 增加：

```
u16 manual_awb_r, manual_awb_g, manual_awb_b; /* milli, 默认 1000 */
bool manual_awb_valid;
```

`s_ctrl` 先更新缓存再尝试写硬件。

### 3.5 s_ctrl 语义

- `V4L2_CID_AUTO_WHITE_BALANCE`：val!=0 → `-EINVAL`；val==0 成功（保持手动）。
- R/G/B：更新对应缓存；三个都非 0 才写硬件。
- 硬件未就绪（`!(isp_state & ISP_START)` 或 suspend）：**返回 `-EAGAIN`，禁止 MMIO**。缓存仍更新，stream-on 再刷。
- 写硬件：

```
hw_r = cached_r * 256 / 1000;
hw_g = cached_g * 256 / 1000;
hw_b = cached_b * 256 / 1000;
/* 拒绝 hw==0：milli=1 时 1*256/1000=0，必须至少写 1 */
if (!hw_r) hw_r = 1; 同 g/b

rkisp_write(..., ISP_PACK_2SHORT(hw_g, hw_g), ISP3X_ISP_AWB_GAIN0_G, ...);
rkisp_write(..., ISP_PACK_2SHORT(hw_b, hw_r), ISP3X_ISP_AWB_GAIN0_RB, ...);
/* 只置位 AWB_ENA，禁止整字覆盖 0x6197 */
set_bits ISP3X_ISP_CTRL0, CIF_ISP_CTRL_ISP_AWB_ENA
必要时置 CIF_ISP_CTRL_ISP_CFG_UPD（对照 isp_params 单 ISP 路径）
```

写寄存器用现有 `rkisp_write` / `rkisp_unite_write` / `rkisp_idx_write`，跟当前 SoC 分支一致。本产品线性 IMX296、单 ISP：只碰 GAIN0。

`g_volatile_ctrl` 不必做；get 走 ctrl 缓存即可。不要从 MMIO 反算当权威（避免 FIL_AIQ 打架）。

### 3.6 重放

在 ISP **STOP→START / stream-on / first cfg 完成之后**（找现有 `ISP_START` 置位点，通常 `rkisp.c` pipeline set_stream）：

- 若 `procfs.mode & (FIL_AIQ|FIL_SW)`：不重放（rkaiq/调试接管）。
- 否则若 `manual_awb_valid`：按缓存写 GAIN0 + enable。

这是冷启动橄榄绿的内核解。不要用加长 gate 代替。

### 3.7 编译/自检

- 本树惯用交叉编译流程见 `DLCVCAM内核编译与更新操作手册.md`。实现阶段按手册编 ISP 相关目标，不要整树无必要 clean。
- `v4l2-ctl -d /dev/v4l-subdevX --list-ctrls` 必须出现：

```
white_balance_automatic 0..1 default=0
red_balance 1..16000 default=1000
blue_balance 1..16000 default=1000
green_balance 1..16000 default=1000
```

ISP subdev 节点名现网用 `media-ctl -p` 找 `rkisp-isp-subdev`。

---

## 4. CamOS 后端

仓：`/home/okay/Desktop/CamOS`。WS 命令名不变：`rkcam_set_balance_ratio`，payload 仍 `{r,g,b}`，**数值基改为 1000**。

### 4.1 下发主路径

文件：`smartcam_backend/camera/rkcam/_runtime/gst_v4l2_controls.py`

`_apply_control_one("set_balance_ratio")`（约 712）：

1. 校验 dict → int r,g,b，clamp 1–16000。
2. `_disable_auto_wb()`（ISP 节点上的 `white_balance_automatic=0`）。
3. 对 **ISP 节点** `_set_ctrl_logical("red_balance"|"green_balance"|"blue_balance")`。
4. 成功才 `_state_set("wb_ratio", {r,g,b})`。
5. **默认不调用** `_write_wb_register_from_ratio`。

`SMARTCAM_WB_REGISTER_ENABLE` 默认改为 **0**。仅该 env=1 时才允许 proc 回退，并打 warning。

删除产品路径对 `mode=0x100` / `400=0x6197` / 地址列表的依赖（代码可留函数但默认死）。

`_wb_ratio_base` 改为 1000。不要再 `ratio/1024*256`。内核已经做 Q8.8。

### 4.2 双节点扫描

文件：`smartcam_backend/camera/rkcam/_runtime/native_v4l2/controls.py`

今天 `_has_sensor_controls`（约 314）把 `exposure/analogue_gain/white_balance_automatic/red_balance/blue_balance` 混成「有一个就算 sensor」。必须拆：

- `control_device`：有 `exposure` 或 `analogue_gain` → IMX296（现 `SMARTCAM_V4L2_CONTROL_DEVICE`）。
- `isp_control_device`：有 `red_balance` 且有 `green_balance` 或 `blue_balance` → ISP。  
  覆盖：`SMARTCAM_V4L2_ISP_CONTROL_DEVICE`。

`set_control_raw` 按 logical 选设备。WB 绝不能写到 sensor 节点。

`gst_v4l2_grabber.py` 的 `_wb_controls_available()` 改为看 ISP 节点，而不是 sensor 上有没有 red_balance。

### 4.3 默认值与 clamp

- `rkcam/models.py`：`DEFAULT_WB_RATIO = {"r": 1572, "g": 1000, "b": 2155}`
- `gst_v4l2_grabber.py`：`SMARTCAM_WB_RATIO_MIN/MAX` 默认 **1 / 16000**（现 256/4096 会裁新单位）。
- `_clamp_wb_ratio(..., default=1000)`；一键 WB 的 `safe_g` 默认 1000。
- `_env_start.py` 打日志时打印新默认。

### 4.4 用户集迁移

文件：`rkcam/_persistence.py`（及 load user set 的唯一入口）

```
if wb_ratio is dict and wb_ratio_unit != "milli":
    convert each of r,g,b: round(v * 1000 / 1024), clamp 1..16000
    写回 wb_ratio 与 wb_ratio_unit="milli"
```

启发式：若三个值都 ≥256 且其中有 1024，几乎肯定是旧基。仍以 **缺 `wb_ratio_unit`** 为权威，不要靠猜。新保存必须带 `wb_ratio_unit`.

### 4.5 生命周期

- `queue_initial_from_state` 继续 **不** 自动写默认 WB（避免和 user set 双写）。注释里删「wb-reg 时序」改成「ioctl + 内核重放」。
- ROI / pipeline 重启后：`queue_control("set_balance_ratio")` 走 ioctl。`_ROI_RECOVER_WB_DELAY_S` 可缩短，**禁止加长**。TODO 轮询首帧可留，但不要再作为必须硬 sleep 的理由。
- 板端若另有 pre-warmup / pre-gate 写 `/proc`（可能不在本 CamOS 树）：改成同一 ioctl，且必须在 ISP_START 之后；ioctl `-EAGAIN` 则短重试，不要 echo proc。

### 4.6 一键 WB

`gst_v4l2_grabber.py` `one_shot_white_balance`：算法保留（预览均值、fixed-G）。

改：

- 种子/clamp 用 1000 基。
- G 锁当前 g（默认 1000），只调 R/B。
- 下发仍 `set_balance_ratio` → ioctl。
- 不要入队旧的 `one_shot_wb` auto-on/off 键。

### 4.7 测试

改/补 `tests/camera/test_gain_refresh_and_roi_recovery.py`：

- **删除或反转**「有 V4L2 也走 wb-reg」。
- 断言 `set_balance_ratio` 调 `_set_ctrl_logical` 三次（或一次打包），**不**调 `_write_wb_register_from_ratio`。
- 迁移单测：`{1610,1024,2207}` → `{1572,1000,2155}` + `wb_ratio_unit=milli`；已是 milli 的不再乘。
- proc enable=0 时写失败只打日志，不抛去打断 drain。

其它 `wb_ratio` / 1024 断言一并改。`gige_vision/test_service.py` 默认 `{1000,1000,1000}`。

---

## 5. CamOS 前端

文件：`frontend/src/pages/Settings/SettingsPage.tsx` `WhiteBalanceRatioEditor`（约 1011）

现在：`cur?.r ?? 1024`，type=number 无 min/max/step，提交原始整数。

改：

1. 缺省显示 **1.000 / 1.000 / 1.000**。
2. 输入按倍率：`min=0.001 max=16 step=0.001`，三位小数。
3. 从状态灌入：`(cur.r ?? 1000) / 1000`。
4. 应用：`setBalanceRatio({ r: round(draft.r*1000), ... })`，再 clamp 1–16000。
5. 文案：「1.000 = 中性增益」。
6. `appStore.setBalanceRatio` 仍发整数，不要在 store 再除一次。
7. `types/ws.ts` 注释：`wb_ratio` 为 milli-gain，1000=1.0。

不要把 1024 留在 UI 默认。

---

## 6. GigE / MVS

现状不一致：

- XML `BalanceRatio` Integer Min=1 Max=16376
- 寄存器默认 **100**（`gvcp_registers.py` 0x0400/404/408）
- 测试服务默认 **1024**
- 同步缺省回退 100

MVS 无法同时把 100 和 1024 理解成 1.0。

改（对齐 SFNC：`BalanceRatio` 是 IFloat）：

1. `gvcp_xml.py`：`BalanceRatio` 改为 **Float**，Min=0.001 Max=16.0 Inc=0.001。  
   IntReg 仍存 milli-int。SwissKnife：`float = reg/1000`，写入 `reg = round(float*1000)` clamp 1–16000。
2. `gvcp_registers.py` 默认 **1000,1000,1000**。
3. `gige_camera_service.py`：与 CamOS `wb_ratio` 整数 milli 直通；去掉「缺省 100」；Once → 现有一键 WB。
4. `test_service.py`：`{1000,1000,1000}`。
5. `BalanceWhiteAuto` Off=0 保持；Continuous 本轮仍可不实现（与内核 auto=EINVAL 一致）。若 MVS 写 Continuous，返回失败或忽略并打日志，不要偷偷开 AWB。

---

## 7. 文档（实现时同步）

CamOS：

- 重写 `docs/18_RKISP白平衡寄存器地址映射.md`：正式路径 = ISP V4L2 milli-gain；proc = debug；写清 538/53c 与 Q8.8。
- `docs/04_gst_v4l2_grabber专题.md`：`set_balance_ratio` 改为 ioctl，不再写「仅寄存器」。
- `docs/02_相机API与WS指令.md`、`docs/07_相机页与设置页控制映射.md`：单位。
- `dlcvcam_demo/docs/dlcvcam_api.md`：`wb_ratio` 含义。
- `docs/CHANGELOG.md` 记一笔。

Kernel 不必长文；控件名/范围写进 `s_ctrl` 旁短注释即可。

---

## 8. 实现顺序（建议）

1. Kernel 控件 + GAIN0 写入 + stream-on 重放。板上 `v4l2-ctl` 先通。
2. CamOS 双节点扫描 + `set_balance_ratio` 改 ioctl + 默认关 proc。
3. 默认值/clamp/迁移/测试。
4. 设置页 1.000。
5. GigE XML/寄存器/同步。
6. 文档。
7. 板端：冷启动首枪、ROI 重启、MVS 1.000、确认无 `mode=0x100`。

Kernel 与 CamOS 分仓提交。CamOS 在未刷新内核的板上：ioctl 失败应打明确错误（找不到 green_balance），**不要静默回退 proc**（除非 env 显式打开）。

---

## 9. 验证清单

1. `media-ctl -p` 找到 `rkisp-isp-subdev`；`v4l2-ctl --list-ctrls` 四项，default 1000 / auto=0。
2. `set-ctrl red=1000,green=1000,blue=1000` 后 `/proc/rkisp*` 只读 `AWBGAIN` 对应 Q8.8≈0x01000100 / 0x01000100。
3. ISP 未 stream 时 set → `-EAGAIN` 或用户态失败，**不 panic**，`dmesg` 无 SError。
4. CamOS WS `rkcam_set_balance_ratio {1572,1000,2155}` 画面接近旧出厂校正；状态回读同值。
5. 设置页默认 1.000；改 1.572/1/2.155 后状态整数匹配。
6. 旧用户集迁一次：1610/1024/2207 → 1572/1000/2155，带 `wb_ratio_unit=milli`。再 load 不再乘。
7. MVS `BalanceRatio` 显示 1.000；改 1.572 后 CamOS r≈1572。
8. close→line0 冷启动第一枪非整幅橄榄（mean 不再接近 72,98,45）。
9. ROI 重启后 WB 仍在，不必加长 sleep。
10. 产品路径 `dmesg` 无 `mode=0x100` / `400=0x6197`。
11. 单测：不再断言 wb-reg 优先。

---

## 10. 关键文件清单

Kernel：

- `drivers/media/platform/rockchip/isp/rkisp.c`
- `drivers/media/platform/rockchip/isp/rkisp.h`
- （对照，尽量不改）`isp_params_v3x.c`、`isp_params_v39.c`、`regs_v3x.h`、`procfs.c`

CamOS：

- `smartcam_backend/camera/rkcam/_runtime/gst_v4l2_controls.py`
- `smartcam_backend/camera/rkcam/_runtime/native_v4l2/controls.py`
- `smartcam_backend/camera/rkcam/_runtime/gst_v4l2_grabber.py`
- `smartcam_backend/camera/rkcam/models.py`
- `smartcam_backend/camera/rkcam/_persistence.py`
- `smartcam_backend/camera/rkcam/_env_start.py`
- `frontend/src/pages/Settings/SettingsPage.tsx`
- `frontend/src/types/ws.ts`（注释）
- `smartcam_backend/camera/gige_vision/gvcp_xml.py`
- `smartcam_backend/camera/gige_vision/gvcp_registers.py`
- `smartcam_backend/camera/gige_vision/gige_camera_service.py`
- `smartcam_backend/camera/gige_vision/test_service.py`
- `tests/camera/test_gain_refresh_and_roi_recovery.py`
- `docs/18_RKISP白平衡寄存器地址映射.md` 等

---

## 11. 实现时的硬约束（复制进 PR）

- 只写 AWB **GAIN0**（538/53c）。
- 只置位 `AWB_ENA`，禁止 `400=0x6197` 整字覆盖。
- milli=1000 → hw=256；hw 算出来为 0 时 clamp 到 1。
- 未 ISP_START 禁止 MMIO。
- CamOS 默认 `SMARTCAM_WB_REGISTER_ENABLE=0`。
- UI/MVS/内核中性点必须都是 1.0/1000，不允许三套单位并存。
