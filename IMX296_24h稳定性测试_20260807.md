# IMX296 24h 稳定性测试 — 总结报告

- **板卡**: `root@192.168.1.180`
- **内核**: `6.1.99-rk3576 #2026080701` / commit `8950d5ff`（分支 `fix/imx296-fast-trigger-n1-pwm-period`）
- **关注点**: 模式切换、ROI set/reset、触发、底部绿线、N-1 相位
- **报告写入**: 2026-08-08（监控已停；板端 endurance 进程当时仍可能存活）
- **原始数据**: 板端 `/tmp/imx296_day_endurance/`（`summary.*` / `bugs.jsonl` / `events.jsonl` / `console.log`）
- **主机快照**: `/tmp/imx296_day_watchdog/`

---

## 1. 测试怎么跑的

### 1.1 两阶段路径（重要）

| 阶段 | 时间（约） | 路径 | 说明 |
|------|------------|------|------|
| A. Grabber 直连 | 08-07 18:14 → 约 22:40 | `GstV4L2Grabber` 占 `/dev/video11` | **错误路径**。未走 CamOS；后与手动 CamOS 抢设备，TOO_FEW/no_frame 爆炸，结果交叉污染 |
| B. CamOS 前后端 | 08-07 22:52 → 监控截止约 08-08 00:57 | `0_测试运行_camos.sh` + API/WS | **正确路径**。login → PATCH `/api/flow_config` → WS `rkcam_*` → `/preview/frame` + `X-Frame-Tag` |

**下文结论以阶段 B（CamOS）为准。** 阶段 A 仅作对照说明，不计入 CamOS 轮次统计。

### 1.2 CamOS 轮次内容（每 cycle）

1. `monitor/close`（free_run）抽几帧  
2. `trigger/software`：BASE LO/HI 相位 + 随机 ROI set/reset + 再 ROI  
3. 同 run_mode 下切 `line0`：BASE + ROI set/reset  
4. 回到 `monitor/close`  
5. 判据：增益固定 0 → LO/HI 曝光交替 → `phase`；JPEG 底行 → 绿线；等新 `X-Frame-Tag`

脚本：worktree `scripts/imx296_day_endurance.py` → 板端 `/tmp/imx296_day_endurance.py`  
输出：`/tmp/imx296_day_endurance/`  
CamOS 轮次起始 pid：`188060`，`--hours 24`

### 1.3 测法注意（已写入脚本）

- **判 N-1 前 `rkcam_set_gain(0)`**：默认用户集增益常 ~12 dB，暗场下会把 LO/HI 对比压扁，假 MIX/LOW_CONTRAST。  
- 曝光：LO≈100 µs / HI≈16000 µs；ROI/模式切换后丢弃 1 枪再采样。  
- 绿线：JPEG RGB 底行启发式（非 NV12 底 48 行；与 grabber 期算法不同）。

---

## 2. CamOS 阶段终态结果

**窗口**: 2026-08-07 22:52:25 → 约 2026-08-08 00:57（uptime ≈ 7519 s，**约 2.1 h**；计划 24h，监控提前结束）

| 指标 | 数值 | 解读 |
|------|------|------|
| cycles | 39 | 后半程切换失败拖慢 |
| cases | 271 | |
| frames / valid | 1894 / 1788 | valid 率 ≈ **94.4%** |
| **green** | **0** | **绿线未复现** |
| **n1** | **1** | 仅 1 条，且对比度极低（见下） |
| mix | 24 | 多为 ROI/暗场相关 |
| lowc | 2 | 曝光几乎无差分 |
| too_few | 15 | 整段采不够有效帧 |
| no_frame | 106 | 单次触发未等到新 tag |
| switch_fail | **17** | **后半程主矛盾** |
| recoveries | 12 | |
| crashes | 0 | 测试进程未崩 |
| bugs 总计 | **57** | 见分类 |

### 2.1 Bug 分类（bugs.jsonl 标题归类）

| 类别 | 约计数 | 代表标题 |
|------|--------|----------|
| 相位 MIX | ~22 | `SW_ROI_*:phase=MIX`、`SW_ROI_RESET:phase=MIX`、`LINE0_ROI_*:phase=MIX` |
| 切换/流失败 | ~17 | `switch_fail:MON` / `MON_retry` / `MON_END` / `LINE0`；`stream_fail:MON*` |
| TOO_FEW | ~15 | ROI 后 / LINE0_BASE / LINE0_ROI_RESET `valid=0/6` 或 `0/8` |
| LOW_CONTRAST | 2 | `SW_BASE:phase=LOW_CONTRAST` |
| N1 | 1 | `SW_BASE:phase=N1` @ cycle 26 |
| green | 0 | — |

### 2.2 对照「内核绿线 / N-1 修复」目标

| 目标 | CamOS 轮结果 | 判断 |
|------|----------------|------|
| 底部绿线 | 0 | **本轮未复现，正面** |
| 明确 N-1 回归 | 1 条边缘样本 | **不能当铁证**（见 §3.2） |
| 长稳切换 + ROI + 触发 | switch_fail / TOO_FEW / no_frame 突出 | **可用性主风险在 CamOS 路径与取帧，不在绿线** |

### 2.3 与阶段 A（Grabber）对照（污染轮，仅参考）

Grabber 直连曾到：cycles≈22，bugs≈307，**n1=7**，green=0，但 **noframe≈1945、valid 卡在 530**——典型 **与 CamOS 抢 `/dev/video11` 后流死亡**，N1/MIX 统计不可信。  
该阶段已归档：`/tmp/imx296_day_endurance_archive/grabber_*.tgz`。

---

## 3. 发现的问题与原因分析

### 3.1 【主问题 A】模式切换失败堆积（后半程主导）

**现象**

- `switch_fail` / `stream_fail` 合计约 17 次。  
- 约 00:00 后大量 `switch_fail:MON` / `MON_retry`，cycle 几乎耗在切换重试上，summary 一度长时间不刷新。  
- 日志里常见：`info=timed out`；有时 sysfs 已是 `active=free_run` 且 `streaming=1`，脚本仍判失败；也有 `streaming=0` 窗口。

**可能原因（按可能性）**

1. **脚本等待条件过严 / 与 CamOS 语义不一致**  
   - `wait_stream(need_free=True)` 要求 `active=free_run`；`need_ft=True` 要求 `master_fast_trigger`。  
   - CamOS `PATCH /api/flow_config` 成功 ≠ 立刻满足脚本假设；冷却 `RUN_MODE_SWITCH_COOLDOWN_S=5s`、硬件 apply 耗时、ROI 后 pipeline 重建都会拉长窗口。  
   - 已 free_run + streaming=1 仍 timeout → 更像 **判定逻辑/超时预算** 问题，而非 sensor 彻底挂死。

2. **CamOS 切换路径偶发卡住或恢复不完整**  
   - `apply_flow_config_patch` 串行 + 硬件 `set_trigger_mode`；失败会 rollback，但前端/脚本若连续狂切，易叠加 cooldown 拒绝与 recover 重启。  
   - recover 会 `pkill server.py` + 再跑 `0_测试运行_camos.sh`（含 **vite build**），一次恢复可达数十秒～数分钟，期间全是 fail。

3. **preview/流在 monitor 边界短暂 streaming=0**  
   - 切换瞬间 grabber 停-起，脚本把「短暂无流」记成 stream_fail。

**影响**: 不是绿线/N-1，但会 **严重稀释 24h 有效 cycle 数**，并把 bugs 数抬高。

**建议改法**

| 层级 | 动作 |
|------|------|
| 测试脚本 | 放宽 `wait_stream`：monitor 只需 `streaming=1`（不必死磕 free_run 字符串）；拉长 settle/timeout；cooldown 显式 sleep ≥5.5s 且对「已是目标模式」短路径成功 |
| 测试脚本 | recover **禁止**动辄整站 `0_测试运行_camos.sh`（会 rebuild 前端）；改为仅 `patch monitor` + `rkcam_reset_roi` + 重连 WS；真死再用轻量 `python server.py` 拉起 |
| CamOS | 给 flow 切换增加「硬件已达目标 + streaming」的统一 ready 查询；超时错误区分 cooldown / hardware fail / stream down |
| 运维 | 长稳前确认单实例 server.py；避免测试中手动再开 CamOS |

---

### 3.2 【主问题 B】触发后无新帧 → no_frame / TOO_FEW

**现象**

- `no_frame=106`，`too_few=15`（整 case valid 不足一半）。  
- 多发于：**ROI set/reset 之后**、`LINE0_BASE` / `LINE0_ROI_RESET`。  
- 机制：`fire_trigger`（WS `rkcam_trigger_once` + `/debug/trigger_once` + sysfs XTRIG）后轮询 `/preview/frame`，要求 **新的 `X-Frame-Tag`**。

**可能原因**

1. **ROI 切换后 pipeline/encoder 重建窗口**  
   - CamOS ROI 会停流重建；脚本 settle 2.5s 可能不够，或首几枪 tag 不涨。  
2. **LINE0 与 software 触发路径不一致**  
   - line0 更依赖外部/XTRIG；仅 WS soft trigger 可能无效；sysfs 脉冲时序与 sensor 窗口不对齐会丢帧。  
3. **preview JPEG 路径与「原始触发帧」不同步**  
   - 测的是编码后 preview，不是 raw NV12；tag 更新策略若合并/跳帧，会表现为「触发了但 tag 不变」。  
4. **暗场 + 极短曝光** 不直接导致 no_frame，但会与相位问题叠加。

**建议改法**

| 层级 | 动作 |
|------|------|
| 测试脚本 | ROI 后：更长 settle（≥ ROI 实测 latency，历史 CamOS ~3.7s）+ 多 discard 帧；LINE0 明确只靠 XTRIG 或只靠已验证的 trigger API |
| 测试脚本 | tag 不变时区分「HTTP 失败 / 旧 tag / 空 body」，写入 bug 字段，便于分流 |
| CamOS | ROI 完成后 ready 信号；trigger_once 与 preview tag 原子绑定（每成功触发必新 tag 或明确 error） |
| 内核/驱动 | 若 ready 后仍系统性丢触发，再查 FT/XTRIG；**先排除 CamOS/脚本** |

---

### 3.3 【次要】相位 MIX / LOW_CONTRAST（多与测法 + ROI）

**现象**

- MIX ≈22，LOW_CONTRAST=2；集中 SW/LINE0 的 ROI 与 RESET。  
- 多数 means 贴在 ~18.3–19.5，**span 常 <1**，暗场 JPEG 对比极差。

**原因**

1. **场景本身极暗**：中心 luma 几乎贴地，LO/HI 差分小，标签抖动 → MIX。  
2. **曝光/增益生效滞后**：WS 异步队列；settle 不足时 HI 帧仍像 LO。  
3. **ROI 后首帧/尺寸变化** 污染序列（已有 1 枪 discard，可能不够）。  
4. **与真 N-1 的区分**：真 N-1 在 **span 足够大** 时 labels 整体错一位；本轮多数 MIX **不具备** 这种清晰对比。

**建议改法**

- 相位用例加可控照明或提高 HI 上限 / 拉长 HI，目标 span≫5。  
- 或对 span&lt;阈值的 case **降级为 inconclusive**，不记 bug（与 green/N1 硬故障分开）。  
- 保持 gain=0；曝光 set 后 wait 可调到 1 帧时间以上。

---

### 3.4 【N-1】唯一样本 — 证据弱

- **唯一**: `SW_BASE:phase=N1` @ **cycle 26**（2026-08-07 23:36:16）  
- means ≈ `[19.024, 18.39, 18.39, 18.391, 18.39, 18.39, 18.39]`，labels 几乎全 LO，span≈0.63  
- **解读**: 更像「只有一点略亮、其余无对比」被算法打成 N1，**不是** grabber 早期那种 span~4、整段错位的经典 N-1。  
- **结论**: **不能单独作为内核 N-1 回归失败的证据**；需在高对比条件下复测。

---

### 3.5 【绿线】本轮未复现

- CamOS 轮 `green_frames=0`，bugs 无 green 类。  
- 注意：本轮绿线判据是 **JPEG 底行 RGB**，与早期 NV12 底 48 行不同；若只关心 ISP raw 底行，建议另加 raw/NV12 探针或内核侧 n1trace/绿线专用 case。  
- 结合阶段 A 在污染前也长期 green=0，**倾向：当前 `#2026080701` 上绿线不是主复发项**。

---

## 4. 根因分层结论（给后续排期）

```
                    ┌─────────────────────────┐
   本轮主噪声 ──►   │ CamOS 切换超时/等待过严  │  → 改脚本判定 + CamOS ready API
                    │ ROI/触发后 preview 无新帧 │  → settle/tag 绑定 + LINE0 触发路径
                    └─────────────────────────┘
                    ┌─────────────────────────┐
   测法噪声  ──►    │ 暗场 JPEG LO/HI 对比不足  │  → 照明/曝光/span 门闩
                    └─────────────────────────┘
                    ┌─────────────────────────┐
   内核目标  ──►    │ 绿线：未复现              │  → 保持观察，可加 NV12 探针
                    │ N-1：1 条弱样本           │  → 高对比专用复现后再定
                    └─────────────────────────┘
```

**一句话**:  
当前 `#2026080701` 在 **CamOS 真实路径** 上，**绿线没出来、N-1 没有可靠复现**；真正拖垮长稳的是 **模式切换失败** 和 **ROI/触发后等不到 preview 新帧**，应优先从 **CamOS + 测试脚本** 修，而不是先再刷一版内核。

---

## 5. 建议的解决顺序（可执行）

### P0 — 让 24h 跑得动、统计可信

1. 改 endurance：`wait_stream` 语义、cooldown、禁止 recover 触发前端 build。  
2. ROI 后 settle ≥ 实测 latency；LINE0 触发路径单一化并打日志。  
3. span 过低的相位结果标 `INCONCLUSIVE`，不计入 n1/mix bug。  
4. 确认板上始终 **单** `python server.py`，测试期间无人手开第二套 CamOS。

### P1 — 验证内核修复是否仍站得住

5. 固定高对比场景（灯/白墙 + gain0 + LO/HI span 目标）跑 software/line0/ROI 各 N 次。  
6. 可选：并行短时 NV12/raw 底行绿线探针，与 JPEG 解耦。  
7. 只有 P0 稳定后，再开真正 24h，并以 green/n1（高对比）为门禁。

### P2 — CamOS 产品侧

8. flow 切换 ready 状态机；ROI/trigger 与 `X-Frame-Tag` 契约。  
9. 避免测试/恢复路径走完整 `vite build`。

---

## 6. 过程时间线（精简）

| 时间 | 事件 |
|------|------|
| 08-07 18:14 | Grabber 版 24h 启动（路径错误） |
| 18:14–22:xx | Grabber：早期有 N1；中后期抢设备 → no_frame/TOO_FEW 主导 |
| ~22:40 | 停 Grabber，归档；CamOS 单实例拉起 |
| **22:52** | **CamOS endurance 启动** pid=188060；冒烟 SW/LINE0 多为 CUR，green=0 |
| 22:52–23:30 | 正常推进；出现 MIX/TOO_FEW；**23:36 唯一 N1（弱）** |
| ~00:00 后 | **switch_fail:MON\*** 连发，有效 case 增速下降 |
| 08-08 ~00:57 | 监控停止时快照：cycles=39 bugs=57 green=0 n1=1 |
| 08-08 | 主机 cron / watchdog 已停；本总结写入 |

---

## 7. 附件与命令

```bash
# 板端结果
ssh root@192.168.1.180 'cat /tmp/imx296_day_endurance/summary.txt; wc -l /tmp/imx296_day_endurance/bugs.jsonl'

# 归档（示例）
ssh root@192.168.1.180 'tar czf /tmp/imx296_day_endurance_archive/camos_$(date +%Y%m%d_%H%M%S).tgz -C /tmp imx296_day_endurance'

# 停板端测试（若仍需要）
ssh root@192.168.1.180 'pkill -f /tmp/imx296_day_endurance.py'
```

- 脚本: `scripts/imx296_day_endurance.py`（CamOS）、`scripts/imx296_day_watchdog.py`（已停）  
- 内核记录入口: 记忆/既有 `IMX296_绿线修复_*`、`IMX296_N1相位滞后_*`  
- 构建号: DLCVCAM `#2026080701`

---

## 8. 最终判定（给决策）

| 问题 | 是否本轮确认 | 优先级 | 主责方向 |
|------|--------------|--------|----------|
| 底部绿线复发 | **否** | 观察 | 内核已像稳住；可选 NV12 补测 |
| N-1 回归 | **未确认**（1 弱样本） | 中（要高对比复测） | 测法 + 必要时内核 |
| 模式切换超时 | **是** | **P0** | 测试脚本 + CamOS |
| ROI/触发失帧 | **是** | **P0** | CamOS preview/trigger + 脚本 settle |
| 暗场相位 MIX | **是（噪声为主）** | P1 | 测法门闩 |

**总体**: CamOS 路径约 2.1h 样本支持「绿线/硬 N-1 不是当前头号问题」；**先修切换与取帧，再重开干净 24h。**
