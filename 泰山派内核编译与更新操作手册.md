# 泰山派 RK3576 内核编译与更新操作手册

> 适用对象：LCKFB TaishanPi 3M (RK3576)  
> 内核版本：6.1.99-rk3576  
> 编译主机：x86_64 Linux (交叉编译)  
> 目标板子：`ssh root@192.168.1.204`

---

## ⚠️ 重要前提

**不要直接在运行中的系统上 `dd` 写入 `/dev/mmcblk0p3`！**

泰山派3M 的 U-Boot 从 eMMC boot 分区加载 **FIT 签名镜像**，新编译的镜像没有原始 RSA 私钥签名，直接 `dd` 写入会导致 U-Boot 验证失败、无法启动（变砖）。

**正确更新方式**：通过 Rockchip 烧录工具（RKDevTool / rkdeveloptool）进入 Loader/Maskrom 模式烧录，或从完整 SDK 的 build 流程生成已签名镜像。

---

## 1. 板子启动结构概览

| 分区 | 设备节点 | 大小 | 用途 |
|------|----------|------|------|
| p1 | `/dev/mmcblk0p1` | 4MB | U-Boot Loader |
| p2 | `/dev/mmcblk0p2` | 4MB | Misc / Trust |
| **p3** | `/dev/mmcblk0p3` | **64MB** | **Boot 分区（FIT 签名镜像）** |
| p4 | `/dev/mmcblk0p4` | 128MB | Recovery |
| p5 | `/dev/mmcblk0p5` | 32MB | Backup |
| p6 | `/dev/mmcblk0p6` | 14GB | Rootfs（Debian） |
| p7 | `/dev/mmcblk0p7` | 128MB | OEM |
| p8 | `/dev/mmcblk0p8` | 206MB | Userdata |

U-Boot 从 p3 加载 FIT 镜像，镜像内包含：
- `kernel`：Linux 内核（lz4 压缩）
- `fdt`：设备树（dtb）
- `resource`：开机 Logo 等资源

FIT 镜像带有 **RSA2048 签名**，U-Boot 启动时强制校验。

---

## 2. 编译环境准备

### 2.1 依赖安装

```bash
sudo apt update
sudo apt install -y build-essential bc bison flex libssl-dev \
    libncurses5-dev libelf-dev gnupg2 wget git u-boot-tools \
    device-tree-compiler lz4
```

### 2.2 交叉编译器

```bash
which aarch64-linux-gnu-gcc
# 输出示例：/usr/bin/aarch64-linux-gnu-gcc
```

---

## 3. 编译内核

### 3.1 基础配置

```bash
cd /home/ypw/kernel

# 加载基础配置
make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- lubancat_linux_rk3576_defconfig

# 合并泰山派3M裁剪配置
./scripts/kconfig/merge_config.sh -m .config \
    arch/arm64/configs/tspi_3m_rk3576_kernel_cut.config

# 应用合并后的配置
make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- olddefconfig
```

### 3.2 编译

```bash
make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- Image dtbs modules -j8
```

> 建议用 `-j8`。如果内存较小（如 8GB 以下）出现 `cc1` 被杀死的情况，请改用 `-j4` 或 `-j2`。

### 3.3 生成 lz4 压缩内核

`scripts/mkimg` 需要 `Image.lz4` 作为输入：

```bash
lz4 -f arch/arm64/boot/Image arch/arm64/boot/Image.lz4
```

---

## 4. 设备树修改（可选）

泰山派3M设备树文件：

```
arch/arm64/boot/dts/rockchip/
├── tspi-3m-rk3576.dts
├── tspi-3m-rk3576-dp.dtsi      # DP显示配置
├── tspi-3m-rk3576-typec.dtsi   # Type-C / DP Alt Mode
├── tspi-3m-rk3576-core.dtsi    # 核心板配置
└── tspi-3m-rk3576-csi*.dtsi    # 摄像头
```

### 4.1 修改 DP 显示路径示例

编辑 `arch/arm64/boot/dts/rockchip/tspi-3m-rk3576-dp.dtsi`：

```dts
&dp0_in_vp0 {
    status = "okay";       // 启用主通道（4K@120Hz）
};

&dp0_in_vp1 {
    status = "disabled";   // 禁用次通道
};

&route_dp0 {
    connect = <&vp0_out_dp0>;  // 路由改为 VP0
};

&vp0 {
    status = "okay";
};

&vp1 {
    status = "disabled";
};
```

修改后重新编译设备树：

```bash
make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- dtbs -j8
```

---

## 5. 打包 boot.img（FIT 镜像）

使用 RK 官方脚本 `scripts/mkimg` 打包：

```bash
cd /home/ypw/kernel

# 生成 boot.img（FIT 格式，含 kernel + dtb + resource）
BOOT_ITS=boot.its ./scripts/mkimg --dtb tspi-3m-rk3576.dtb
```

成功后会生成：
- `boot.img`（~23MB，FIT 镜像）
- `resource.img`（Logo 等资源）

验证：

```bash
mkimage -l boot.img
```

---

## 6. 烧录到板子

### 方式一：RKDevTool（Windows，官方推荐）

1. 下载 [RKDevTool_v3.32](https://wiki.lckfb.com/zh-hans/tspi-3-rk3576/img-download/distributed-image.html) 并解压
2. 下载泰山派3M分区表 `TaishanPi-3M-RK3576_Linux.cfg`，导入 RKDevTool
3. 勾选 **boot** 分区，选择生成的 `boot.img`
4. 板子进入 **Loader 模式**：
   - 按住 **REC** 按钮不放
   - 按一下 **RST** 复位键
   - 等待 2 秒后松开 **REC**
5. 点击 RKDevTool 的"执行"按钮烧录

### 方式二：rkdeveloptool（Linux 命令行）

安装工具：

```bash
sudo apt install libusb-1.0-0-dev
# 从 https://github.com/rockchip-linux/rkdeveloptool 编译安装
git clone https://github.com/rockchip-linux/rkdeveloptool.git
cd rkdeveloptool && autoreconf -i && ./configure && make && sudo make install
```

烧录步骤：

```bash
# 1. 板子进入 Maskrom 模式（短接 Maskrom 测试点 + 上电）
#    或进入 Loader 模式（REC + RST）

# 2. 确认设备连接
sudo rkdeveloptool ld
# 应显示：DevNo=1 Vid=0x2207,Pid=0x350b,Maskrom

# 3. 下载并烧录 boot.img
sudo rkdeveloptool db /path/to/RK3576Loader.bin   # 下载 Loader
sudo rkdeveloptool wl 0x8000 boot.img              # 写入 boot 分区（地址见分区表）
sudo rkdeveloptool rd                               # 重启
```

> 地址 `0x8000` 来自 `TaishanPi-3M-RK3576_Linux.cfg` 分区表中 boot 分区的起始偏移。

---

## 7. 救砖/回滚

如果更新后无法启动，使用上述**写入 boot 分区**的方法重新烧录即可：

- **方式一**：使用 RKDevTool（Windows）重新烧录原厂固件或分散镜像中的 `boot.img`
- **方式二**：使用 `rkdeveloptool`（Linux 命令行）重新烧录：

```bash
# 板子进入 Loader/Maskrom 模式后执行
sudo rkdeveloptool ld
sudo rkdeveloptool db /path/to/RK3576Loader.bin
sudo rkdeveloptool wl 0x8000 boot.img
sudo rkdeveloptool rd
```

> 如果新镜像仍有问题，可烧录原厂备份的 `boot.img` 恢复。

---

## 8. 完整编译更新脚本

在编译主机上创建 `build_bootimg.sh`：

```bash
#!/bin/bash
set -e

cd /home/ypw/kernel

echo "[1/4] 编译内核..."
make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- Image dtbs modules -j8

echo "[2/4] 生成 Image.lz4..."
lz4 -f arch/arm64/boot/Image arch/arm64/boot/Image.lz4

echo "[3/4] 打包 boot.img..."
BOOT_ITS=boot.its ./scripts/mkimg --dtb tspi-3m-rk3576.dtb

echo "[4/4] 完成！"
ls -lh boot.img

echo ""
echo "下一步：通过 RKDevTool 或 rkdeveloptool 烧录 boot.img"
```

---

## 附录：常用排查命令

```bash
# 查看 FIT 镜像内容
mkimage -l boot.img

# 查看 eMMC 分区表
fdisk -l /dev/mmcblk0

# 查看当前内核日志中的显示相关日志
dmesg | grep -i "vop\|dp\|hdmi\|drm"

# 查看 USB-C / DP 状态
dmesg | grep -i "typec\|fusb302\|dp_alt"
```
