# 文档总目录

## 摘要

本目录用于记录鲁班猫 RK3588 内核树中的设备树、驱动配置和板级适配信息，避免后续重复搜索上下文。

## 文档清单

| 文档名称 | 主题说明 | 适用模块 | 关联功能 | 更新时间 |
| --- | --- | --- | --- | --- |
| `docs/00_文档总目录.md` | 文档入口与索引 | 全局 | 全部 | 2026-04-01 |
| `docs/plans/2026-03-24_imx296_cam1_minimal_enable_plan.md` | IMX296 cam1 最小可编译可加载修复计划 | Rockchip DTS/媒体驱动配置 | IMX296 cam1 | 2026-03-24 |
| `docs/plans/2026-03-25_imx296_runtime_mode_switch_plan.md` | IMX296 运行模式 `echo` 切换计划，当前板级默认已切到 `master_fast_trigger`，并已补上 DTS 默认模式与 `V4L2` 控件缓存同步修复 | Rockchip 媒体驱动 / sysfs | IMX296 cam1 | 2026-04-02 |
| `docs/plans/2026-03-26_imx296_pwm_trigger_plan.md` | IMX296 使用 `GPIO1_D6(PWM14_M2)` 输出低电平单脉冲的设计与验证计划，当前板级默认低脉宽为 `800us` | Rockchip 媒体驱动 / DTS / PWM / sysfs | IMX296 cam1 PWM trigger | 2026-04-01 |
| `docs/plans/2026-03-27_imx296_trigger_dev_switch_plan.md` | trigger-dev 默认目标由 OS08A20 切换到 IMX296 的实施计划与验证约束 | Rockchip DTS overlay / 输入驱动 / sysfs | IMX296 外部硬触发链路 | 2026-03-27 |
| `docs/plans/2026-03-30_trigger_dev_dual_input_mode_plan.md` | trigger-dev 增加 `button/edge` 双输入模式与命令行切换能力的设计与验证计划，并补充 RK GPIO V2 运行时切到单沿时的 `int_bothedge` 修复约束；当前板级默认输入模式为 `edge` | 输入驱动 / GPIO 控制器 / DTS overlay / sysfs | 按键安全触发 / 外部脉冲触发 | 2026-04-01 |
| `docs/06_IMX296_cam1_适配记录.md` | IMX296 cam1 适配实现、验证结果、`run_mode` + `trigger-dev(GPIO0_C6)` 到 `GPIO1_D6(PWM14_M2)` 触发链路，以及默认 `master_fast_trigger` / `800us` / `input_mode=edge` 的板级说明，现已补齐 `run_mode` 首次写入 no-op 修复记录 | Rockchip 相机链路 / GPIO 控制器 / DTS overlay / 媒体驱动 / PWM / trigger-dev | IMX296 cam1 | 2026-04-02 |
| `docs/plans/2026-03-25_gd32_i2c_device_plan.md` | GD32 `0x2a` 正式 I2C 设备接入计划 | I2C / DTS / misc driver | GD32 MCU | 2026-03-25 |
| `docs/07_GD32_I2C设备接入.md` | GD32 `0x2a` 的正式 I2C 设备接入、构建验证与板端调试入口 | I2C / DTS / overlay / misc driver | GD32 MCU / IMX296 辅助链路 | 2026-03-25 |

## 说明

- 当前项目原先未建立根级 `docs/`，本次先补最小入口和与 IMX296 任务直接相关的文档。
- 后续若继续扩展相机链路适配，应优先在现有专题文档上增量更新，而不是重复新建同主题文档。
- `IMX296` 触发输出目前已同时覆盖 `run_mode` 切换和 `GPIO1_D6(PWM14_M2)` 单脉冲入口；后续若继续优化，应优先更新 `docs/06_IMX296_cam1_适配记录.md`，不要再拆分同主题说明。
- `GD32` 相关后续若进入协议驱动阶段，应优先更新 `docs/07_GD32_I2C设备接入.md`，不要重复新建同主题文档。
