## IMX415 外部触发拍照（信息梳理/已实现：同步从模式 + 单帧触发接口）

> 目标：把 IMX415 改成“外部信号触发一次 -> 输出一帧图像”的工作方式；MIPI-CSI 链路与 V4L2 获取图像方式保持不变。  
> 备注：本文已根据提交 `5ee51a81e24fb4f7170d6e6e8fd3cf9a0501eacb`（imx415-test）更新同步从模式（slave）相关实现说明，并补齐单帧触发接口（sysfs + V4L2 control）。

## 现状（已测信号与像素信息）

### 外部同步/触发信号（现状测得）

- **XVS**：30Hz，低电平触发，inversed，占空比 0.04%，宽度 14.7us  
- **XHS**：67.5kHz，低电平触发，inversed，占空比 1.45%，宽度 210ns
- **电平**：**1.8V 逻辑电平**（XVS/XHS 电压不得超过 IMX415 的 OVDD/IOVDD=1.8V）

### 传感器像素信息（现状提供）

- **Total number of pixels**：3864(H) × 2228(V)
- **Number of recommended recording pixels**：3840(H) × 2160(V)
- **Optical black**：H 前 0 后 0；V 前 36 后 0
- **Dummy**：H 前 0 后 0；V 前 1 后 1

## 板端 PWM 模拟（XVS / XHS）

板端使用两路 PWM 输出模拟同步信号：

- **PWM14-m2**：GPIO1_D6（RK_PD6）
- **PWM15-ir-m3**：GPIO1_D7（RK_PD7）

当前的 sysfs 配置：

```sh
# 配置端口
echo 0 > /sys/class/pwm/pwmchip1/export
echo 0 > /sys/class/pwm/pwmchip2/export

echo 33333750 > /sys/class/pwm/pwmchip1/pwm0/period
echo 14000 > /sys/class/pwm/pwmchip1/pwm0/duty_cycle
echo 1 > /sys/class/pwm/pwmchip1/pwm0/enable

echo 14815 > /sys/class/pwm/pwmchip2/pwm0/period
echo 200 > /sys/class/pwm/pwmchip2/pwm0/duty_cycle
echo 1 > /sys/class/pwm/pwmchip2/pwm0/enable

# 查看信息
cat /sys/kernel/debug/pwm
```

两路 PWM 的地址/参数对应关系为：

- **platform/febf0020.pwm（PWM14）**：period 33333750ns duty 14000ns polarity inverse（对应 XVS≈30Hz/14us，且满足 `XVS = XHS * 2250`）
- **platform/febf0030.pwm（PWM15）**：period 14815ns duty 200ns polarity inverse（对应 XHS≈67.5kHz/200ns）

> 注意：当 IMX415 处于 slave 同步模式时，应由板端（PWM 或外部源）驱动 XVS/XHS；驱动侧已做“传感器端口 Hi-Z”处理，避免与板端输出冲突（见下文“同步从模式实现”）。

## 设备树关联（LubanCat-5 cam1 IMX415）

### 顶层 DTS 入口

- `arch/arm64/boot/dts/rockchip/rk3588-lubancat-5.dts`
  - 引入 `rk3588-lubancat-5-csi.dtsi`
  - 默认引入 `rk3588-lubancat-5-cam1-imx415.dtsi`（cam1 IMX415）

### 相机公共时钟/电源定义

- `arch/arm64/boot/dts/rockchip/rk3588-lubancat-cam.dtsi`
  - `ext_cam_37m_clk`：fixed-clock，37125000Hz
  - `cam_avdd/cam_dovdd/cam_dvdd`：固定电源（2.8V/1.8V/1.2V）

该文件由 `rk3588-lubancat-5-csi.dtsi` 顶部 `#include` 引入。

### cam1 的 IMX415 传感器节点（定义在 CSI dtsi 内）

文件：`arch/arm64/boot/dts/rockchip/rk3588-lubancat-5-csi.dtsi`

- cam1 使用 **I2C5**，IMX415 节点标签为：`dcphy1_imx415: dcphy1-imx415@1a`
- 关键属性（摘要）：
  - `compatible = "sony,imx415"`
  - `reg = <0x1a>`
  - `clocks = <&ext_cam_37m_clk>; clock-names = "xvclk"`
  - `avdd-supply/dovdd-supply/dvdd-supply = <&cam_avdd/...>`
  - `pwdn-gpios = <&gpio6 8 GPIO_ACTIVE_HIGH>`
  - `reset-gpios = <&gpio6 9 GPIO_ACTIVE_LOW>`
  - `port/endpoint`：`data-lanes = <1 2 3 4>`（4lane）

> 说明：`&gpio6` 来自 `rk3588-lubancat-5.dts` 里的 PCA9535 GPIO 扩展（I2C5 上的 `gpio-expander@21`），所以 cam1 的 `pwdn/reset` 不是 SoC 原生 GPIO。

### cam1 的 MIPI-CSI/ISP 链路拓扑（remote-endpoint 串起来的真实路径）

cam1（dcphy1）链路在 `rk3588-lubancat-5-csi.dtsi` 中是这样连的：

1. `dcphy1_imx415` 输出端点 `imx415_dcphy1_out`
2. → `&csi2_dcphy1` 输入端点 `dcphy1_in_imx415 (endpoint@4)`
3. → `&csi2_dcphy1` 输出端点 `csidcphy1_out`
4. → `&mipi1_csi2` 输入端点 `mipi1_csi2_input`
5. → `&mipi1_csi2` 输出端点 `mipi1_csi2_output`
6. → `&rkcif_mipi_lvds1` 端点 `cif_mipi_in1`
7. → `&rkcif_mipi_lvds1_sditf` 端点 `mipi_lvds1_sditf`
8. → `&rkisp1_vir0` 端点 `isp1_vir0`

### cam1 使能方式（dtsi vs overlay）

两种方式做的事情本质一致：把一串节点 `status = "disabled"` 改为 `"okay"`。

- **静态编进 DTB（dtsi）**：
  - `arch/arm64/boot/dts/rockchip/rk3588-lubancat-5-cam1-imx415.dtsi`
  - 内容主要是对以下节点置 `okay`：
    - `&i2c5`, `&dcphy1_imx415`, `&csi2_dcphy1`, `&mipi1_csi2`, `&mipi_dcphy1`
    - `&rkcif`, `&rkcif_mipi_lvds1`, `&rkcif_mipi_lvds1_sditf`, `&rkcif_mmu`
    - `&rkisp1`, `&isp1_mmu`, `&rkisp1_vir0`

- **运行时 overlay（dtbo）**：
  - `arch/arm64/boot/dts/rockchip/overlay/rk3588-lubancat-5-cam1-imx415-overlay.dts`
  - 同样是把上述节点置 `okay`

通过 overlay 选择主/从模式（二选一，不可同时加载）：

| 模式 | dtbo | 说明 |
|------|------|------|
| **从模式** | `rk3588-lubancat-5-cam1-imx415-overlay.dtbo` | 外部 XVS/XHS 触发，`echo 1 > /sys/bus/i2c/devices/.../trigger` 拍一帧 |
| **主模式** | `rk3588-lubancat-5-cam1-imx415-master-overlay.dtbo` | 传感器内部连续出图，常规录像/预览 |

在 **uEnv.txt**（或板卡对应的 uEnv 如 `uEnvLubanCat5.txt`）中只启用其中一个：

```bash
# 从模式（触发单帧）
dtoverlay=/dtb/overlay/rk3588-lubancat-5-cam1-imx415-overlay.dtbo

# 主模式（连续出图），与上面二选一
#dtoverlay=/dtb/overlay/rk3588-lubancat-5-cam1-imx415-master-overlay.dtbo
```

若使用从模式，仅配置 `dtoverlay=rk3588-lubancat-5-cam1-imx415-overlay.dtbo` 即可，无需再单独配置。

## PWM14 / PWM15 设备树关联（用于输出 XVS/XHS）

### PWM 控制器节点（SoC）

当前版本中，`rk3588-lubancat-5-cam1-imx415-overlay.dts` **已在同一个 overlay 内启用 `&pwm14/&pwm15` 并选择 `pwm14m2_pins/pwm15m3_pins`**，因此只要启用 cam1 IMX415 overlay，即可自动输出 XVS/XHS。

如果只想单独启用 PWM 引脚（不启用相机 overlay），仍可使用：
`rk3588-lubancat-pwm14-m2-overlay.dtbo` + `rk3588-lubancat-pwm15-ir-m3-overlay.dtbo`。

文件：`arch/arm64/boot/dts/rockchip/rk3588s.dtsi`

- `pwm14: pwm@febf0020`（默认 `status = "disabled"`）
- `pwm15: pwm@febf0030`（默认 `status = "disabled"`）

### PWM 引脚复用（pinctrl）

文件：`arch/arm64/boot/dts/rockchip/rk3588s-pinctrl.dtsi`

- `pwm14m2_pins`：`<1 RK_PD6 ...>` → **GPIO1_D6**
- `pwm15m3_pins`：`<1 RK_PD7 ...>` → **GPIO1_D7**

### PWM overlay（启用 + 选择复用组）

- `arch/arm64/boot/dts/rockchip/overlay/rk3588-lubancat-pwm14-m2-overlay.dts`
  - `target = <&pwm14>; status = "okay"; pinctrl-0 = <&pwm14m2_pins>;`
- `arch/arm64/boot/dts/rockchip/overlay/rk3588-lubancat-pwm15-ir-m3-overlay.dts`
  - `target = <&pwm15>; status = "okay"; pinctrl-0 = <&pwm15m3_pins>;`

对应的 uEnv 配置位置：

- `arch/arm64/boot/dts/rockchip/uEnv/uEnvLubanCat5.txt`
  - `#dtoverlay=/dtb/overlay/rk3588-lubancat-pwm14-m2-overlay.dtbo`
  - `#dtoverlay=/dtb/overlay/rk3588-lubancat-pwm15-ir-m3-overlay.dtbo`
  - `#dtoverlay=/dtb/overlay/rk3588-lubancat-5-cam1-imx415-overlay.dtbo`
  - `#dtoverlay=/dtb/overlay/rk3588-lubancat-5-cam1-imx415-1080p-overlay.dtbo`

## 同步从模式（slave）实现（已合入）

### DT：声明 IMX415 为 slave 同步模式

提交 `5ee51a81...` 在 cam1 IMX415 overlay 中为传感器节点增加：

- `rockchip,camera-module-sync-mode = "slave";`

文件：

- `arch/arm64/boot/dts/rockchip/overlay/rk3588-lubancat-5-cam1-imx415-overlay.dts`

该属性的 key/value 定义在：

- `include/uapi/linux/rk-camera-module.h`
  - `RKMODULE_CAMERA_SYNC_MODE = "rockchip,camera-module-sync-mode"`
  - 支持字符串：`"internal_master" / "external_master" / "slave"`

### 驱动：stream-on 时配置 XMASTER + XVS/XHS Hi-Z

驱动新增 `imx415->sync_mode`，并在 `__imx415_start_stream()` 里调用 `imx415_apply_sync_mode_regs()`。

- **寄存器 0x3003（XMASTER）**：
  - bit0：`0=master`，`1=slave`
- **寄存器 0x30C1（XVS/XHS drive）**：
  - `[3:2]=XHS`、`[1:0]=XVS`
  - slave 模式下将上述 4bit 全置 1（`0x0f`），把 XVS/XHS 输出驱动切到 Hi‑Z，避免和板端 PWM 输出冲突

文件：

- `drivers/media/i2c/imx415.c`

### 运行时 ioctl（Rockchip rkmodule）

驱动实现了以下 subdev ioctl：

- `RKMODULE_GET_SYNC_MODE`
- `RKMODULE_SET_SYNC_MODE`

定义位置：

- `include/uapi/linux/rk-camera-module.h`（`enum rkmodule_sync_mode`）

> 注意：`imx415_apply_sync_mode_regs()` 目前在 stream-on 流程里执行；如果运行时通过 ioctl 修改了 sync_mode，一般需要重新 stream-on 才能确保寄存器按新模式生效（取决于上层是否会在切换后重启传感器流）。

## 驱动关联（drivers/media/i2c/imx415.c）

### DT 属性读取（关键）

驱动在 `imx415_probe()` 里读取这些属性：

- `rockchip,camera-hdr-mode`：HDR/Linear 模式选择（默认 NO_HDR）
- `rockchip,imx415-data-lanes`：lane 数（默认 4；2lane 则用 `supported_modes_2lane`）
- `rockchip,imx415-capture-mode`：默认分辨率选择（默认 0，即 4K）
- `rockchip,camera-module-sync-mode`：同步模式（`internal_master` / `external_master` / `slave`；默认 `NO_SYNC_MODE`）

### 分辨率/裁剪关系（与“推荐录制 3840×2160”对应）

驱动的主模式表里，线性 10bit 默认模式为 **3864×2192**（raw 输出尺寸），同时在 `get_selection()` 中提供裁剪框：

- 3864×2192 → 裁剪到 **3840×2160**
- 1944×1096 → 裁剪到 **1920×1080**

### 与“触发/单帧”相关的现有控制点（先记录）

- **开停流**：`IMX415_REG_CTRL_MODE (0x3000)`
  - `__imx415_start_stream()`：写 `0`（streaming）
  - `__imx415_stop_stream()`：写 `1`（standby）
- **快速开停流 ioctl**：`RKMODULE_SET_QUICK_STREAM`
  - 直接写 `0x3000` 进入 streaming/standby（不重配整套寄存器）
- **帧长/帧率**：VTS/VMAX 寄存器 `0x3024/0x3025/0x3026`
  - `V4L2_CID_VBLANK` 会更新 VTS
- **曝光/增益**：
  - 曝光（LF）`0x3050~0x3052`（通过 `V4L2_CID_EXPOSURE` 写 SHR0=VTS-Exp）
  - 增益（LF）`0x3090/0x3091`（`V4L2_CID_ANALOGUE_GAIN`）
- **BRL**：驱动里定义 `BRL_ALL = 2228`，并在 `RKMODULE_GET_SONY_BRL` 中返回（与总行数 2228 对应）

### 单帧触发（已实现：sysfs + V4L2 控制）

目标：在 **slave 同步模式** 下，通过“软件触发一次 -> 输出 1 次 XVS（帧触发）-> 产生 1 帧”实现单帧拍照；同时保持 XHS（行同步）连续输出，确保每帧满足固定的 XHS 次数要求。

#### 每帧需要的 XHS 次数（VMAX）计算

以当前 4K@30（驱动默认 linear 10bit 3864×2192 输出裁剪到 3840×2160）为例：

- **已测时序**：`XHS = 67.5kHz`，`XVS = 30Hz`
- **每帧 XHS 次数**：\(N_{XHS/Frame} = \frac{XHS}{XVS} = \frac{67500}{30} = 2250\)

这个 2250 也可以直接在驱动模式表中得到验证：

- `drivers/media/i2c/imx415.c` 里该模式 `vts_def = 0x08ca = 2250`（对应 **VMAX/VTS**）
- `BRL_ALL = 2228`（总读出行数）
- 因此 **垂直空行**：\(VBLANK = VMAX - BRL = 2250 - 2228 = 22\)

结论：每帧 **需要 2250 个 XHS 周期**（即 VMAX/VTS），并配合 **1 次 XVS 脉冲**作为帧边界/触发。

#### 实现点（DT + 驱动）

- **DT（cam1 IMX415 overlay）**：为 IMX415 节点绑定两路 PWM（XVS/XHS）
  - `pwm-names = "xvs", "xhs";`
  - `pwms = <&pwm14 0 33333750 PWM_POLARITY_INVERTED>, <&pwm15 0 14815 PWM_POLARITY_INVERTED>;`（按 `XVS = XHS * 2250` 取整）
  - `rockchip,xvs-duty-ns = <14000>;`
  - `rockchip,xhs-duty-ns = <200>;`
  - **电平要求**：XVS/XHS 必须为 **1.8V 逻辑电平**
- **驱动（drivers/media/i2c/imx415.c）**
  - probe：读取 `rockchip,xvs-duty-ns/rockchip,xhs-duty-ns`，并通过 `devm_pwm_get_optional()` 获取 `xvs/xhs`
  - stream-on/off：在 `sync_mode=slave` 时 enable/disable **XHS**（持续输出行同步/时钟）
  - trigger：
    - 输出 1 次 **XVS** 脉冲作为帧触发
    - 内部按 `cur_vts`（默认 2250）计算 1 帧时间，限制两次触发的最小间隔，保证 “每帧 2250 个 XHS 周期” 的关系不被打乱

#### 触发接口

- **sysfs**：`trigger`（write-only，写入脉冲数）
  - 路径：`/sys/class/video4linux/v4l-subdevX/device/trigger`
  - （稳定路径）cam1 I2C5@0x1a：`/sys/bus/i2c/devices/5-001a/trigger`
  - 用法：
    - `echo 1 > .../trigger`：输出 **1 个** XVS 脉冲（单帧触发）
    - `echo 2 > .../trigger`：输出 **2 个** XVS 脉冲（间隔 1 帧时间，可用于测试/验证）
- **V4L2 control**：`user_trigger`（button，write-only）
  - 用法：`v4l2-ctl -d /dev/v4l-subdevX -c user_trigger=1`

#### 使用流程（示例）

- 终端 A：启动采集并等待一帧（示例以 `/dev/video0` 为例，按实际 video 节点调整）
  - `v4l2-ctl -d /dev/video0 --stream-mmap --stream-count=1 --stream-to=frame.raw`
- 终端 B：触发一次
  - `echo 1 > /sys/class/video4linux/v4l-subdevX/device/trigger`
  - 或 `v4l2-ctl -d /dev/v4l-subdevX -c user_trigger=1`

> 重要注意：
> - 触发必须在 pipeline 已经 `STREAMON` 时执行，否则 sysfs/V4L2 trigger 会返回忙（EBUSY）。
> - 如果驱动占用了 PWM（通过 DT `pwms` 绑定），同一个 PWM 不能再用 `/sys/class/pwm/*` 方式导出配置，否则会互相冲突；period/polarity 在 `pwms` 里，duty 在 `rockchip,xvs-duty-ns/rockchip,xhs-duty-ns` 或驱动默认值。
> - **电平**：XVS/XHS 为 **1.8V 逻辑电平**，板端 PWM 输出电压不得超过 1.8V（必要时加电平转换或改用 1.8V IO 域引脚）。

## 相关文件索引（本次已读/已定位）

### 设备树/Overlay

- `arch/arm64/boot/dts/rockchip/rk3588-lubancat-5.dts`
- `arch/arm64/boot/dts/rockchip/rk3588-lubancat-5-csi.dtsi`
- `arch/arm64/boot/dts/rockchip/rk3588-lubancat-cam.dtsi`
- `arch/arm64/boot/dts/rockchip/rk3588-lubancat-5-cam1-imx415.dtsi`
- `arch/arm64/boot/dts/rockchip/overlay/rk3588-lubancat-5-cam1-imx415-overlay.dts`（新增 `rockchip,camera-module-sync-mode="slave"` + `pwm-names/pwms` + `rockchip,xvs-duty-ns/rockchip,xhs-duty-ns`）
- `arch/arm64/boot/dts/rockchip/overlay/rk3588-lubancat-5-cam1-imx415-1080p-overlay.dts`
- `arch/arm64/boot/dts/rockchip/overlay/rk3588-lubancat-pwm14-m2-overlay.dts`
- `arch/arm64/boot/dts/rockchip/overlay/rk3588-lubancat-pwm15-ir-m3-overlay.dts`
- `arch/arm64/boot/dts/rockchip/uEnv/uEnvLubanCat5.txt`
- `arch/arm64/boot/dts/rockchip/rk3588s.dtsi`
- `arch/arm64/boot/dts/rockchip/rk3588s-pinctrl.dtsi`

### 驱动/头文件（同步/触发相关）

- `drivers/media/i2c/imx415.c`
- `include/uapi/linux/rk-camera-module.h`（`RKMODULE_CAMERA_SYNC_MODE` / `RKMODULE_GET_SYNC_MODE` / `enum rkmodule_sync_mode`）
- `drivers/media/platform/rockchip/cif/capture.c`（上层通过 `RKMODULE_GET_SYNC_MODE` 获取传感器同步模式，用于多路同步配置）


