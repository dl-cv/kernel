# IMX296 绿线/串帧修复 — 内核修改与长期验证记录

> 长期记忆与人工复核文件。本文件随每次新修改、新发现持续更新。
> 最后一次更新：2026-08-06 13:40（内核 procfs 门闩已刷板 `#2026080601`，accept/reject/skip 冒烟通过）

---

## 0. 问题背景

IMX296 在 **trigger 模式（master_fast_trigger）** 下，尤其是 free-run → trigger 切换、ROI 修改/重置后的最初几帧，出现 **绿线（底部残帧/串帧）**。根因不在 sensor 曝光本身，而在 ISP 通路帧完成与 buffer 交接的竞态。

测试痛点：板端无硬件触发源时通过 sysfs `run_mode` + WebSocket 软触发驱动。真正有意义的 bug 几乎都出现在 **模式切换 / ROI 修改** 时。

---

## 1. 根因分析（结论）

IMX296 FT（fast trigger / XTRIG）下 MIPI 帧「保持打开」直到下一次触发到来，造成：

1. **ISP V39 提前完成（early-done）竞态**：`wait_line` 在 height−8−1 处触发 `ISP3X_OUT_FRM_HALF`，但此时 **MI 的 Y/CB 写尚未完成**，尤其 FT 帧被 XTRIG 提前关闭时，chroma（CB）最后几行可能还没落盘。
2. **V39 的 ISP_OUT_LINE 提前饱和**：约 8 行提前饱和，不能作为完成依据。
3. **MI Y/CB_OFFS_CNT_SHD 可能残留上一帧 final 值**：FT 的 SOF 到来时 MI 的 SHD 寄存器还持有上一帧的写计数，导致「读到 full」就立即放行的假象。
4. **进入 FT 后第一帧**：上一轮 free-run 的打开帧 + 首个 XTRIG，invalid 帧关闭与首个 valid 帧交接。
5. **`rkisp_start` 里 `stream->skip_frame` 会被清零**，所以「丢掉头 N 帧」不能靠它，需要独立的持久计数器。

---

## 2. 内核修改文件清单

| 文件 | 修改点 | 作用 |
|---|---|---|
| `drivers/media/platform/rockchip/isp/capture.h` | 新增 early-done 状态字段与 API | 结构体字段：`early_done_delay_us/timer/pending/armed_sof_ns/poll_us/terminal_line/deadline_ns_lo/hi/mi_y_size/mi_cb_size/mi_saw_y/mi_saw_cb/mi_ready/post_mi_us/warmup_left/warmup_post_mi_us/skip_frames/drop_left`。API：`rkisp_stream_buf_done_early_drain`、`rkisp_early_done_clear_state` |
| `drivers/media/platform/rockchip/isp/capture.c` | MI 写计数缓存、`rkisp_early_done_mi_finished()`、定时器轮询后 hold | 读取 MI Y/CB_OFFS_CNT_SHD 得到 `mi_y_size/mi_cb_size`；判定「live ramp（saw_y/saw_cb 低于目标值）+ full」才 ready；ready 后按 `post_mi_us` 再 hold 若干 µs 让 chroma 尾行落盘；超 residual 上限只记日志，不强制 `vb2_done` |
| `drivers/media/platform/rockchip/isp/capture_v39.c` | `mi_frame_end` 里 early-done 前丢弃帧 | 若 `early_done_drop_left > 0`：递减、requeue buffer、goto end（不触发 vb2_done），可跨 `stream->skip_frame` 清零存活 |
| `drivers/media/platform/rockchip/isp/rkisp.c` | 0518 策略常量与 FT 启动逻辑 | TAIL=8、residual 上限默认 15000（max 40000）、line_us 80–250、poll 50µs、POST_MI=6000µs、WARMUP_POST_MI=16000µs、WARMUP_FRAMES=4、SKIP_EARLY_FRAMES=3、SKIP_BUF_FRAMES=3 |
| `drivers/media/i2c/imx296.c` | 进入 FT 的双 dummy 触发 | `imx296_dummy_trigger_locked()` 两次（日志 `enter-fast-trigger` / `enter-fast-trigger-2`），关闭进入 FT 后残留的 invalid 打开帧 |

---

## 3. Early-done 完成策略（capture.c 核心逻辑）

```
FT SOF →
  early_done_skip_frames > 0 ? 不做 early-done（走真实 MI FE IRQ），递减
  : 在 wait_line(OUT_FRM_HALF) 处 arm early_done 定时器
定时器每 poll(50µs) 轮询一次：
  MI Y_OFFS_CNT_SHD / CB_OFFS_CNT_SHD 与 programmed size 比较
  saw_y/saw_cb 记录「这一臂里见过 < size 的值」→ 证明不是残留 SHD
  只有 (saw_y && saw_cb && Y_full && CB_full) → mi_ready=1
  mi_ready 后计时 post_mi_us（FT 用 6000，warmup 用 16000）再完成 vb2_done
residual 超上限：仅 dev_dbg 告警，绝不强制 vb2_done（避免残帧出流）
```

设计要点：
- **不信任单次 SHD 读值**，必须看到 live ramp（从低于 size 增长到 size）才认为本帧 MI 真写完。
- **不做「超时强制 done」**。残帧宁可丢（被 drop_left / skip 掉），也不能带着未写完的 chroma 出流形成绿线。
- **post-MI hold** 让最后几行 NV12 色度落盘后再上报帧完成。
- warmup（进入 FT 后前 `WARMUP_FRAMES=4` 帧）用更保守的 `WARMUP_POST_MI=16000µs`。
- `SKIP_EARLY_FRAMES=3`：前 3 个 SOF 完全交给 MI FE IRQ。
- `SKIP_BUF_FRAMES=3`：`stream->skip_frame` 额外丢 3 个 FT 头帧（free-run→FT 后首个 XTRIG 帧最脏）。
- `early_done_drop_left`（capture_v39）提供**跨 start 清零存活的丢弃**。

---

## 4. Free-run 路径处理

free-run 下 **不 arm early-done**（`rkisp_early_done_clear_state` 清理状态），完全走真实 MI FE IRQ 完成，保持出厂路径不变，避免误伤 free-run 稳定性。

---

## 5. 测试方法论（board: root@192.168.1.180）

- **CamOS**：`/root/smartcam_bs/0_测试运行_camos.sh`，软触发走 sysfs + WS。
- **连续采集脚本**：`/root/roi_endurance_live.py`（持久副本；`/tmp/roi_endurance_live.py` 是运行副本）。
  - 每个 sample：软触发 → 等 `X-Frame-Tag` 变化（**绝不用 hold-last 预览当新帧**）→ 存 `frames/`。
  - free-run 与 trigger 两模式都连续存图。
  - 事件集：`random_roi`（随机尺寸+随机偏移+重置全幅）、`roi_thrash`、`mode_flip`（free↔trigger↔monitor）、`min_settle`。
- **绿线判定**：每帧底部区域 RGB，任一通道异常偏高记为 green；绿帧存 `green/`。
- **MISS 判定**：触发后未见新 X-Frame-Tag。
- **skip_free**：ROI 变更后 free-run 的前 4 帧丢弃（`skip_free_left=4`），因实测 free tag=1（变更后首帧）确为脏帧。

### 关键运行参数（board 端）
```
sysfs run_mode: /sys/bus/i2c/devices/4-001a/run_mode
free_run / master_fast_trigger / monitor
capture node: /dev/video11 (rkisp_mainpath)
API: http://127.0.0.1:8000/api/auth/login  {"username":"admin","password":"dlcv2026"}  → {"success":true}
```

---

## 6. 长期验证进展（更新记录）

### 6.1 内核构建 `#2026080518`（6.1.99-rk3576）
- 首次承载本修复全量策略（MI live+full、post-MI hold、skip/drop、dummy×2）。
- 已刷入板端 flash（多次重启后仍生效，验证通过）。

### 6.2 长时间 endurance 结果（多段）
| 时间段 | free 存图 | trig 存图 | green | miss | roi 次数 | mode 次数 | 结论 |
|---|---|---|---|---|---|---|---|
| 首段 ~18:00–20:30（含 3 次板端重启） | 累计增长 | 累计增长 | **0** | 0 | 数十 | 上百 | 稳定，重启为网络/系统问题非 bug |
| 20:30–00:40 段（单进程 4535） | ~20.7k | ~8.2k | **0** | 58（集中在 00:11 一次 burst 超时卡死窗口，非绿线） | 212 | 309 | 稳定；miss 为 API/burst 瞬时超时 |
| 00:40 板端重启后新周期（10h，监控至 09:13 结束） | 跨 4 次重启（00:40/05:02/06:54/08:52）多段累计 free≈1.2 万+、trig≈5 千+（每段被重启清零重新累计） | 同左 | **0** | 0（各恢复后段） | 数百（含随机偏移） | 数百 | 全段 green=0；重启为 systemd 看门狗 30s 触发（非绿线） |

**核心结论：green=0 已跨多段、多 ROI 组合、多模式切换、多板端重启保持。** 偶发 MISS 均为 API 软触发/网络瞬时故障导致的自愈事件，**与绿线无关**。

### 6.3 典型自愈事件（记录留档）
- **20:11 MISS 风暴**：ROI `1456x400_0_474` 后触发 MISS（46 次），free 帧恢复正常。手动复位：kill endurance + CamOS → 重启 → 恢复。
- **00:11 burst 超时**：`event_reset_roi → shoot_burst` 抛 `TimeoutError`，seq 卡死 25437，watchdog 判 stall → 重启 CamOS；endurance 自愈 recover，miss=58 冻结不再增长。
- **00:40 板端重启**：内核保留（flash 验证），CamOS 自启，endurance 需手动重启（见 §7 命令）。
- **04:32:50 例行恢复**：api=False → 04:33:31 自愈 api=True，green=0，seq=24858。00:11 后第 5 次同类 API 瞬时故障（00:11/01:36/02:45/03:03/04:32），全部自愈，miss 计数自 02:45 后冻结在 158 不再增长。均为软触发/API 链路瞬时问题，与绿线无关。
- **04:36:18 例行恢复**：api=False（seq 冻结 24947）→ 04:36:59 自愈 api=True，green=0，seq=25582（追帧正常）。第 6 次同类事件，~40s 自愈。**观察：04:32–04:36 窗口内两次 API 瞬时故障，软触发/API 链路抖动频率略有上升**，但 green 恒 0、seq 持续推进，未影响帧质量（详见 §8 待办）。
- **04:38:21 例行恢复**：api=False → 04:39:02 自愈 api=True，green=0，seq=25659。第 7 次同类事件（04:32/04:36/04:38 三连），全部 ~40s 内自愈，小时报确认板端 streaming=1 正常。**结论：API 链路抖动加剧但自愈极快，帧质量不受影响；10h 周期内暂不需介入，周期结束后建议单独排查 CamOS 软触发链路（§8）。**
- **04:46:35 例行恢复**：api=False → 04:47:17 自愈 api=True，green=0，seq=26588，~42s。第 8 次事件，04:32 起约 15 分钟内 4 次抖动（~1次/5min），全部自愈，watchdog 全程正确 defer 不双杀。**green 恒 0、seq 每次恢复后推进，帧质量不受影响。**
- **04:50:01 例行恢复**：api=False（seq 一度冻结 26588 达 ~5min）→ 04:50:43 自愈 api=True，seq=26644（恢复推进）。第 9 次事件，04:32–04:50 约 18 分钟内 5 次抖动，全部 ~40s 自愈，watchdog 按 streak 逻辑 defer、未误判。**链路抖动持续但帧质量保持 green=0。**
- **05:02:30 板端重启（第 2 次）**：/tmp 整体清空 → endurance/camos 全灭，watchdog 判定 endurance dead 并自动重启。手动恢复：CamOS 拉起（i=18 OK）、endurance PID 4279、END_TS 保留原 10h 窗口（1785984201，至 08-06 10:43）、left=5.66h。**dmesg/journal 无 panic/oops/watchdog 记录**，重启原因不明；两次重启（00:40、05:02）均发生在 API 抖动之后，疑为板端外部复位（硬件看门狗/供电/外部指令），与绿线无关（green 全程 0）。**待办：查板端复位来源（§8）。**
- **05:27 小时报新发现（ROI→recover 触发条件定位）**：重启后（05:03 起）7 次 recover 全部在 `roi set_*` 后 ~15s 触发（05:15/05:19/05:22/05:25 均如此）。即 endurance 在 ROI 变更后的健康检查（等新 X-Frame-Tag）超时，判定异常 → 重启 CamOS。恢复后 seq 持续推进、green=0、miss=0。**结论更新：API 抖动主因是 ROI 变更后的软触发链路超时自愈，非随机故障；与绿线无关，但每次 ROI 变更耗 ~60-90s 恢复窗口。**
- **06:54:32 板端重启（第 3 次，根因确认）**：/tmp 清空 → 恢复流程同前（endurance PID 4090、left=3.8h、END 保留）。**诊断结论**：`/etc/systemd/system.conf.d/10-dlcvcam-watchdog.conf` 配置 `RuntimeWatchdogSec=30s`，systemd（PID 1）持有 /dev/watchdog0 每 30s 喂硬件看门狗（2ace0000）。**三次重启（00:40/05:02/06:54）均发生在重度流切换/API 抖动之后，机制为：系统阻塞 >30s（软锁/D-state 风暴）→ 硬件复位**。上次启动日志在流状态正常后无 panic 结束，符合看门狗复位。重启间隔缩短（4.4h→1.9h）与 recover 频率上升同步。**与绿线无关（green 全程 0）。**
- **08:52:10 板端重启（第 4 次）**：/tmp 清空 → 恢复（endurance PID 4361、left=1.84h、END 保留至 10:43）。重启间隔稳定在 ~2h（00:40/05:02/06:54/08:52）。**看门狗 30s 机制下，重度流切换期间系统 >30s 阻塞反复触发硬件复位；每次重启清零已积累数据，但 green=0 在所有跨重启周期保持。**

---

## 7. 运维命令速查

### 板端
```bash
# 重启 endurance（10 小时）
OUT=/tmp/roi_endurance_live; mkdir -p $OUT/frames $OUT/green
END_TS=$(( $(date +%s) + 36000 )); echo $END_TS > $OUT/end_ts
cp -f /root/roi_endurance_live.py /tmp/roi_endurance_live.py
setsid nohup env ENDURANCE_OUT=$OUT python3 /tmp/roi_endurance_live.py --hours 10 >> $OUT/console.log 2>&1 < /dev/null &

# 看状态
cat /tmp/roi_endurance_live/summary.txt
tail -n 20 /tmp/roi_endurance_live/console.log
```

### 本机 watchdog（dev 机）
```bash
# 常驻：/tmp/endurance_watchdog.py  (PID 385900)，每 40s 采集
# 输出：/tmp/endurance_watchdog_state/status.json
cat /tmp/endurance_watchdog_state/status.json
```
Watchdog 行为：API 优先健康判定；endurance 自愈窗口（recover）不抢杀；seq stall>360s → CamOS 重启；endurance 死 → 重启（若未过 end_ts）。

### 内核
```bash
# 编译 & 刷板（见 DLCVCAM内核编译与更新操作手册.md）
# 本仓库最新 fix 提交：416f32d22c72 fix(imx296): close FT frame and harden V39 early-done drain
```

---

## 8. 后续关注点（待办/风险）

- [x] 00:40 重启后新 10h 周期跑完，复核绿色 0 与 roi/mode 覆盖率（09:13 监控结束判定 green=0 可交付）。
- [ ] 若再次出现 free tag=1 绿线，考虑把 skip_free 的丢弃逻辑前移到内核（而非测试脚本）。
- [x] **板端重启根因复核（2026-08-06 10:10，纠正先前“仅 30s 看门狗误复位”判断）**：
  - 配置属实：`/etc/systemd/system.conf.d/10-dlcvcam-watchdog.conf` → `RuntimeWatchdogSec=30s`，`systemd` PID1 持有 `/dev/watchdog0`（`2ace0000.watchdog`）。设计意图是 hard hang → 停 pet → SoC 复位 → U-Boot boot-try。
  - **pstore 实锤不是“单纯 >30s 软锁”**：`/var/lib/systemd/pstore/dmesg-ramoops-0` 捕获
    `Kernel panic - not syncing: Asynchronous SError Interrupt`，
    `Comm: python`，`pc: vfree` / `lr: rkisp_proc_write+0xf8`（`drivers/.../isp/procfs.c` 写路径末尾 `vfree(buf)`；SError 多为此前 `writel`/`rkisp_write` 非法访问 ISP 寄存器的异步报错）。
    同一签名在 `dmesg.txt`（build `#2026080504`）也出现过 → 可复现，非偶发。
  - **崩溃前序（重度流切换 thrash）**：反复 `stream on/off` → `MIPI_CSI2 ERR2` → 大量 `CIF_ISP_PIC_SIZE_ERROR` → `start pipeline failed -22` → 再 `run_mode=master_fast_trigger`（streaming=0 仍打触发）→ userspace 写 rkisp procfs：`mode=0x100`、`400=0x6197` → SError panic。另有 `rkisp_stream_stop id:0 timeout`。
  - **重启形态分类**：
    - boot -2/-3/-4/-5：journal 无 `System is rebooting`，末尾停在流错误/PIC_SIZE → **硬死/panic 后复位**（看门狗是收尸，不是首因）。
    - boot -1（09:40）：有 `systemd-logind: System is rebooting` → **软件 reboot**（endurance 周期尾/人工/脚本），与 panic 不同。
  - **处置建议**（改配置仍需用户确认）：
    1. **优先修根因**：CamOS/python 经 `/proc` 写 ISP 寄存器的时机与 ISP power/clock/pipeline 状态；以及 ROI/mode thrash 下 `PIC_SIZE_ERROR` / `pipeline failed -22` / `stream_stop timeout`。
    2. **RuntimeWatchdogSec=30s 可临时放宽到 2–5min 作辅助**，但 **放宽 alone 挡不住 SError panic**；panic 路径仍会复位。仅当确认存在“无 panic 的长阻塞”时放宽才有独立收益。
    3. 保留 pstore/ramoops；下次复位后优先 `cp /var/lib/systemd/pstore/dmesg-ramoops-*` 归档。
- [ ] **ROI→recover 触发条件已定位**：endurance 在 ROI 变更后等新 X-Frame-Tag 超时（~15s）即重启 CamOS。建议排查 endurance 的 ROI 变更后健康检查超时阈值/触发逻辑（与绿线无关，但影响测试效率，每次 ROI 变更耗 ~60-90s）。
- [ ] MISS 风暴根因在 API/WS 软触发链路，与绿线无关，但值得单独跟进稳定性（可选）。
- [ ] 内核层面可将 warmup 帧数/skip 数做成可调参数，便于回归调参。
- [x] **rkisp_proc_write SError 写者定位（2026-08-06）**：CamOS `smartcam_backend/camera/rkcam/_runtime/gst_v4l2_controls.py` → `V4L2ControlsRoiManager._write_wb_register_from_ratio()`（默认 `SMARTCAM_WB_REGISTER_ENABLE=1`）。写入序列：
  1. `mode=0x100`（`RKISP_PROCFS_FIL_AIQ`）
  2. `400=0x6197`（`ISP3X_ISP_CTRL0`，开 AWB 等）
  3. `538/53c/...` AWB gain（`ISP3X_ISP_AWB_GAIN*_G/RB`）
  调用链：`set_balance_ratio` / load user set；路径 `/proc/rkisp-vir0`。**无 streaming 门闩**，ROI/mode thrash teardown 窗口仍会写 → SError。板端已确认 **V4L2 无 red/blue balance**，不能靠关 WB-reg 代替（会丢手动白平衡功能）。
- [x] **最小功能保留修复（板端 CamOS，2026-08-06 10:57 已落地并重启验证）**：同文件加 streaming/ISP 门闩，**不关** `SMARTCAM_WB_REGISTER_ENABLE`：
  - 默认 `SMARTCAM_WB_REGISTER_REQUIRE_STREAMING=1`
  - 写 MMIO 前要求：非 `camera_offline`，且 `run_mode streaming=1` 或 `/proc/rkisp*` 显示 `Isp online/offline`（内核 `ISP_START`）；否则 skip 并打 `[wb-reg] skip MMIO (...)`
  - `set_balance_ratio` **先写入 state 目标 ratio**，MMIO 失败也不丢用户设置（ROI/mode 恢复后 grabber restore 会再写）
  - 备份：`gst_v4l2_controls.py.bak.20260806_serror_gate`
  - 验证：streaming=1 时 write=True（启动加载用户集2 `r=1383 g=1024 b=2000` 成功）；`camera_offline` / `streaming=0` / `isp_not_start` 时 write=False；`/preview/frame` JPEG 正常；未关 WB 功能。
- [x] **ROI/mode thrash 门闩验证（2026-08-06 10:59:55–11:21:38，~22 min）**：
  - 输出：`/tmp/roi_endurance_wb_gate`，`ENDURANCE_OUT=... python3 ... --hours 0.33`
  - **结果**：`free=772 trig=565 green=0 miss=0 roi=17 mode=29 recover=8 camos_rst=4 err=0`，存图 1337 帧，`green/` 空
  - **无板端重启**：`boot_same=y`，uptime 连续，dmesg 无 SError/panic，pstore 未刷新新 panic（仍为 08:51/09:41 旧档）
  - 事件覆盖：`random_roi×8`、`roi_thrash×2`、`mode_flip×2`、`reset_roi×2`、`min_settle×1`
  - **WB 功能保留**：streaming 窗口内多次 `[wb-reg] write ... ratio r=1383 g=1024 b=2000`（`camos_server_wb_gate.log` 14 条 + camos_restart 后 `camos_boot.log` 7 条）；门闩代码仍在板端
  - **残留（非 panic）**：ROI 后 endurance 健康检查仍触发 recover/camos_rst（与绿线/SError 无关，测试效率问题）
- [x] **内核门闩补丁已刷板（2026-08-06 13:37，`#2026080601`）**：`drivers/media/platform/rockchip/isp/procfs.c`
  - 镜像：`boot-rk3576-6.1.99-2026080601-416f32d2-dirty.img`（sha256 `33eff39a…b516244`），`dd` → `/dev/disk/by-partlabel/boot`
  - 含绿线策略工作区变更 + procfs 门闩（ISP_START + RPM get/put + reg `<0x6000` 且 4 字节对齐 + 写长度 cap）
  - **冒烟**：
    1. streaming=1：`mode=0x100` / `400=0x6197` / `538`/`53c` → rc=0，dmesg 正常 `rkisp_proc_write`，**无 skip**
    2. 坏寄存器：`6000=0x1`、`401=0x1` → rc≠0，dmesg `reject bad reg`
    3. CamOS 停、streaming=0：`400=…`/`538=…` → `-ENODEV`，dmesg `skip MMIO (isp_state=0x10001 …)`（**mode= 仍可写**，只拦 FIL_AIQ/FIL_SW MMIO）
    4. CamOS 重启后：gain/pulse 可调（gain=55、pulse≈8986）、preview JPEG OK、CamOS `[wb-reg] write` 仍成功；dmesg 无 SError/panic
  - 与 CamOS streaming 门闩双层防护；watchdog 未改

---

## 9. 结论

当前内核策略在长期混合负载（free+trigger、随机 ROI 含偏移、模式频繁切换、板端多次重启）下 **green=0 稳定保持**。修复核心：**MI 真实完成（live ramp + full）+ post-MI hold + 早期 SOF skip + 持久 buffer drop + 进入 FT dummy 触发**。测试脚本侧以 skip_free 丢弃 ROI 变更后脏 free 帧为辅。

**判定标准**：单段 ≥ 数小时、覆盖多种 ROI（含随机偏移）与模式切换、green=0 且 free/trig 连续存图正常，即可判定修复稳定可交付。**2026-08-06 09:13 监控结束：00:43 起 10h 周期累计 ~8.5h（含 4 次板端重启、数十次 CamOS 重启、数百 ROI/mode 切换），全程 green=0，判定修复稳定。**

**SError / WB-reg（2026-08-06）**：硬复位主因更正为 thrash 窗口 CamOS 经 `/proc/rkisp-vir0` 写 ISP 寄存器触发 Asynchronous SError。**双层修复已上板**：① CamOS streaming/ISP 门闩（不关 WB-reg，thrash ~22min 无复位）；② 内核 `procfs.c` 门闩 build `#2026080601` 已刷 180，accept/reject/skip 冒烟通过。`RuntimeWatchdogSec` 未改。

**遗留问题**（均与绿线无关，见 §8）：① ROI 变更后 endurance 健康检查超时自愈；② 可选更长 thrash 提高 no-SError 置信度；③ `#2026080601` 为 dirty 树构建（含未提交绿线/procfs 工作区改动），合入前需正式 commit。
